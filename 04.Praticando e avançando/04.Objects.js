const person = {
    name: 'John',
    age: 30,
    wight: 88.6,
    isAdim: true
}

console.log(person.name)
console.log(person.age)
console.log(person.wight)
console.log(person.isAdim)

console.log(`${person.name} tem ${person.age} anos e tem ${person.wight}kg.`)
